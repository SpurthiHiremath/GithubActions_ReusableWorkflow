name: Dev Deployment

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  
jobs:
  build:
    uses: SpurthiHiremath/GithubActions_ReusableWorkflow/.github/workflows/reusable-maven-build.yml@main
    with:
      environment: dev
    secrets: inherit
