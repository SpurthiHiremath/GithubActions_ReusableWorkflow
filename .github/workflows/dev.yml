name: Dev Deployment

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  
jobs:
  deploy:
    uses: ./.github/workflows/reusable-maven-build.yml
    with:
      environment: dev
